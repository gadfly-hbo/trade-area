# AGENTS.md

## 项目概述

商圈对比分析网页：将多个商圈按统一维度横向对比，用于**线下门店选址决策**，行业聚焦**服饰零售（成人装 + 童装为主）**。设计容量 2800 个商圈。用户以中文沟通，页面文案与数据均为中文。

## 技术栈与命令（npm，Node ≥ 20）

- 纯静态架构：React 19 + Vite 8 + TypeScript 7 + Ant Design 6 + ECharts 6，无后端。
- `npm run etl` — 解析商圈数据源（见下）生成 `public/data/`（index.json 全量摘要 + details/s{N}.json 分片，每片 50 个商圈）；数据更新 = 重跑此命令。
- `npm run dev` — 开发服务器（vite.config.ts 已设 `server.host: true` 对外监听；本工作区经 Remote-SSH 访问，用户浏览器需用远程主机 IP（100.106.28.2）+ 端口打开，`localhost` 只在远程主机本机有效）；`npm run build` — tsc --noEmit + 生产构建；`npm run typecheck`；`npm test`（vitest，ETL golden 测试）。
- 路径别名 `@/` → `src/`。`src/data/loader.ts` 是前端唯一取数模块（换后端只改这里）。路由用 HashRouter（静态部署免服务端配置）。

## 关键实现约定

- xlsx 是 CJS 包：Node ESM 下具名导出在 `default` 上，`scripts/parse-xlsx.ts` 顶部有兼容导入，勿改回 `import * as XLSX` 直接用。
- TS 7 已移除 `baseUrl`，tsconfig `paths` 用相对路径（`./src/*`）。
- 评分与雷达图统一使用百分位分（ETL 期算好存入 index），逆向指标（租金、竞品数）在百分位层已翻转，「越高越优」全局一致；评分模型在 `src/scoring/model.ts`，ETL 与前端共用，改权重逻辑只改这一处。
- 评分容缺：某因子缺失时其权重按比例分摊给其余因子，不因缺数据惩罚。

## UI 设计规范（JuanerAI Prism 棱镜，必须遵循）

经用户确认全面切换到全局设计规范 `~/.zcode/design/DESIGN.md`（JuanerAI Prism 棱镜工作台，version alpha）。**浅色 SaaS、墨青主色，固定浅色（Prism 未定义暗色，勿加回 ◐ 主题切换）**；此前的「控制台版式 + 黑白灰暗色」已弃用（更早前绿色系也已弃用）。令牌与组件形态落地在 `src/theme.ts` 与 `src/index.css`：

- **色板**：bg `#f5f7fa` / surface `#ffffff` / surface-2 `#f9fafb` / surface-3 `#eef2f6` / text `#17202a` / muted `#5d6b7d` / soft `#8a96a6` / border `#dce3ea` / border-strong `#c2ccd8`；主色 primary `#155e75`（墨青，仅主操作/选中态/焦点环/关键数字）+ primary-soft `#e2eff3` / primary-ink `#0c3b4a`；语义色 green/amber/red/teal/violet 成对 `-soft` 底色，只表达状态。
- **版式**：左侧 240px 粘性侧边栏（品牌区 + 分组导航「工作台」+ 计数胶囊 + 底部身份区）；主区结构固定：页头（标题 + 一句用途说明 + 至多一个主按钮）→ 指标格 → 主工作区 → 页脚边界说明行。
- **字体**：Inter / PingFang SC 等；页面标题 21px/600、分区标题 15px/600、正文 14px/1.55、辅助 11.5px；字重只用 400/500/600；数字 `font-variant-numeric: tabular-nums`（`.num`）。
- **组件形态**：状态徽标 = 圆点 + 文字（`.pill`，六态 green/warn/bad/info/violet/brand/neutral，`.no-dot` 去圆点）；卡片/面板 8px 圆角、按钮/输入 6px；层级用背景色+边框表达，不用大阴影（浮层例外）；指标卡 `.metric`。
- **图表**：ECharts 基础片段 `chartBase(palette)`，多系列取色 `CHART_SERIES`（墨青/青绿/紫罗兰/琥珀）。
- 新增页面/组件时优先用 `Section`（面板+标题形态）、`MetricCard`、`Badge`，不要引入 AntD 之外的第三套视觉。

## 数据源与结构

**每个文件对应一个商圈**。数据源为本仓库 `data/raw/NNN_商圈名.md`（当前 2405 份研究报告，**随 git 入库**，双端 `git pull` 后启动脚本检测到源数据更新会自动重跑 ETL）；开发时可用环境变量 `TRADE_AREA_MD_DIR` 指向研究报告工作目录（如相邻项目 `../trade-area-data/district-12dim`）；目录内无 md 时 ETL 退回旧格式 xlsx（两者不同时启用以免商圈重复；历史 xlsx 样本保留在 `tests/fixtures/` 作 golden 测试夹具）。

- md 内表格三种排版变体需兼容：2 列、3 列（带序号列）、行名加粗；行名允许空白变体（如「周边 3 公里人口」）。尾部批次只有 12 维（缺「项目名称与地址」行），商圈名以文件名为准。
- xlsx 为单 sheet（`12维度竞品分析表`），布局 A1:B15：第 1 行标题，第 2 行列头，第 3–15 行为 13 行数据。
- 固定行序：项目名称与地址 / 项目性质 / 所在区位 / 商业级别与体量 / 开业时间 / 周边3公里人口 / 核心客群 / 日均客流 / 交通条件 / 核心定位 / 最大优势 / 最大短板 / 业绩参考。
- 描述列为长文本，内嵌结构化标记，**解析时必须保留、展示时可样式化**：
  - 数据来源：`[检索·来源名]`（md 中有 `[检索·x B]`、`[检索·x] A级` 等变体）；可信度分级：`[A]`（权威）/ `[B]`（次级）
  - 项目评级：`【项目评级】X`，森马五级 S/A+/A/B/C（types.ts `ProjectRating`）。md 原无此标记，已由 `scripts/backfill-md.ts` 从 `data/reference/渠道项目管理清单.xlsx`（按商圈名匹配）回填到各 md 的项目性质行；更新渠道清单后重跑该脚本（默认 dry-run，`--apply` 生效，幂等）再 `npm run etl`。前端将评级作为独立字段呈现（详情页信息栏+维度明细首行、列表「项目评级」列、导出 xlsx 指标表），不并入项目性质正文（清洗时该标记已从正文剔除）。指标标签：`【新增指标·xxx】`；字段分隔符：`｜`
  - 核心客群维度含"森马三大人群"预测：A类｜质感流行派 / B类｜都市体面家 / C类｜百搭优选客，带 `±pp` 置信区间；提取锚定「字母+类?+竖线/括号?+人群名」，兼容空格/无竖线/无约字/区间中值/「百达优选客」错别字，三项合计偏离 100%±15 整组弃用
- 省市解析允许「市而无省」（沈阳市…）与直辖市裸名（上海闵行区…）；空省市在 ETL 内用全量「城市→省」共识回填，不引入外部地理数据。
- 客流指标统一为万人次/日：md 口径含「万/日」「万人」「5,500-9,000人次/日」等，裸人次按除以万换算；数字必须带「万/人次/人」单位，无单位不取（例外：三档裸区间可继承总句单位，如「日均客流约5,000–8,000人次，其中工作日3,000–5,000、周末8,000–15,000」，单位取锚词前方最近者）。锚词连接类含斜杠/顿号/括号/约号~/比较符>/加粗*；峰值锚词含裸「节假日/节庆」与「峰值+日期…日均」复合式。防污染：「销售/营收/营业额」措辞与「峰值/周末」前邻「销」不取；任一档 ≥100 万人次/日不取（步行街/全域口径）。
- 3公里人口三层匹配：合计锚 → 圈层/半径/覆盖锚（「商圈」不算，常住/人口/居住/估算关键词必选）→ 裸「(约)N万人」兜底；兼容约后空格、万省「人」、「合计推断约 35 万」插字；「人口密度」句与 >300 万（全市/全区口径）不取。
- 推断口径（报告标注[推断]/推测）**取值并标记**：extractMetrics 第二出参收集 inferred，随 index.json/details 下发，列表/对比/详情在数字旁显示「推断」警示标（悬停说明）；仅租金「未公开」句、销售额混入与全域口径例外（硬拒）。
- **停用指标（2026-09-25 用户决策）**：租金、竞品商业体、商户数、首店数已从前端全部移除，租金同时退出评分模型（评分四因子：客流/3公里人口/客群匹配/停车便利）；四者的提取逻辑与数据（含 overrides）仍保留在 JSON 中，恢复展示只需改前端。
- 租金为街铺窄通道口径（商铺语境：临街/街铺/商铺/底商/首层/商圈等）：四级递进——带「租金」词锚定句 → 直述铺租（「临街铺138元/㎡/月」）→ 裸「租金」句（「租金通常40-80元/㎡/月」）；日租直取、月租÷30 换算，坪效/物业费/写字楼/公寓句与超域值（0.1–100 元/㎡/天 之外）不取；`元/㎡/年` 几乎全是坪效，勿混入。
- 竞品数限 2/3km 圈层紧口径（含 ≥/约前缀），已停用展示、不在评分模型；品牌数为独立展示字段 `brands`（品牌≠商户），与 `merchants` 分离。
- 体量类指标（2026-09 扩展）：建面认 总建面/商业面积/体量 + 万㎡/万方/万平方米（纯 ㎡ 大数折万）；车位兼容 近/超/区间/「2181个停车位」数字前置；商户兼容 超/近前缀与「870家商户」；首店含「首进」、数字前置，「首店率/占比」无数值不取；开业年份优先完整年月日 → 「正式开业」紧邻年 → 任意「开业/试营业」紧邻年；品牌数含 无动词正向（数字后必须有 +/余/多/个/家 之一）与数字前置反向。
- 语义核定覆盖 `data/overrides.json`（商圈名 → 指标 → {value, inferred}）：由 LLM 逐条语义判定（14 并行子代理，规则见 `reports/_audit/`）+ 合理域校验生成，`build-data.ts` 仅在正则未提取到该指标时应用。峰值语义 = 日峰值（累计口径取括注日均）。空值排查报告 `reports/空值排查报告.md` 列出剩余两类空值（①语义判定无数据 ②待人工附原句）。

## 数据已知修正（勿回退）

- 爱山广场 2023 年零售额同比为 **+11.3%**（147 → 163.6 亿元），不是 +10.8%。
- "湖州站"必须区分：**普铁老站**距项目约 11.7km；**沪苏湖高铁新站**（2024-12-26 开通）距约 3km。
- 单店级销售/租金/坪效数据未公开，属数据缺口，展示时标注"未披露"，勿编造。

## 选址分析视角（服饰：成人装 + 童装）

对比页面应突出与服饰选址相关的指标，而非泛商业展示：

- 客流三档（工作日 / 周末 / 节假日峰值）、夜间客流占比、顾客停留时长
- 3 公里人口与客群结构、与三大人群（质感流行派 / 都市体面家 / 百搭优选客）的匹配度
- 租金水平（口径示例：街铺挂牌 1.11 元/㎡/天 vs 万达档口 3.89 元/㎡/天）
- 车位/客流比、2–3 公里竞品密度、首店数量与品牌梯队
